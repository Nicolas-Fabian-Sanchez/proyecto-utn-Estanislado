import './Pedido.css'
import Carta from '../Carta/carta';

const input = document.querySelector("input");
const producto = document.querySelector("label");

input.addEventListener("click",porciones);

export default function porciones(){
    
        const lista = document.createElement("input type='number'");
        lista.classList.add("number")
    
        
}